import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Egg Shop - Register',
  description: 'Halaman Register',
};

const Page = () => {
  return <h1>Register Page</h1>;
};

export default Page;
