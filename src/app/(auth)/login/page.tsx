import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Egg Shop - Login',
  description: 'Halaman login',
};

const Page = () => {
  return <h1>Login Page</h1>;
};

export default Page;
